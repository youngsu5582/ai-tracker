// chrome-extension/src/config.js

/**
 * Global configuration for the AI Activity Tracker extension.
 */
const config = {
    API_ENDPOINT: "http://localhost:11240/api/data/capture",
    DEBUG_MODE: true // Set to true for development, false for production
};