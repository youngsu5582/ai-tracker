spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC

    show-sql: true
  ai:
    openai:
      api-key: sk-proj-test
#  threads:
#    virtual:
#      enabled: true